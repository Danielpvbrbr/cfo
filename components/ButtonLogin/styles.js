import { StyleSheet } from 'react-native';

export default css = StyleSheet.create({
  button: {
    width: '90%',
    height: 60,
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 5,
    color: '#fff',
    fontWeight: 'bold',
    marginTop:7,
    marginBottom:7,
  },
  text: {
    color: '#fff',
    fontSize: 18,
    fontWeight: 'bold'
  }
});
